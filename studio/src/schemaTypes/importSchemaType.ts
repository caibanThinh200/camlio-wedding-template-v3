// Secondary block registry — blocks added via `npm run create-block`
// The scaffold script appends imports and array entries here automatically.
import { footer } from './objects/footer'

export const importSchemaTypes = [
  footer,
  // ↓ scaffolded blocks are appended below this line
]
