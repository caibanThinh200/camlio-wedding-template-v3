// Extra page-builder array entries for blocks added via `npm run create-block`
// The scaffold script appends entries here automatically.
export const importPageBuilderTypes: { type: string }[] = [
  { type: 'footer' },
  // ↓ scaffolded blocks are appended below this line
]
