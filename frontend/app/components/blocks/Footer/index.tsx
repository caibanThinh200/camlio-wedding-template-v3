// Footer lives in components/global/Footer.tsx
// This re-export keeps the block schema import path consistent if needed.
export { default, type FooterProps as FooterBlockProps } from '@/app/components/global/Footer'
